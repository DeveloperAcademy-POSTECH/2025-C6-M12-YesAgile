/*
 ===================================================================================
 [File Name] : JourneyAddViewModel.swift
 [Role]      : 여정 추가 및 수정 화면의 입력 상태를 관리하는 뷰모델
 [Layer]     : ViewModel (Form Logic)
 ===================================================================================
 
 [핵심 책임 (Core Responsibilities)]
 1. **Form State**: 사용자가 입력 중인 메모(`memo`), 선택한 날짜(`date`), 선택한 이미지(`selectedImage`)를 관리합니다.
 2. **Location Holding**: 이미지에서 추출된(혹은 사용자가 지정한) 위치 정보(`coordinate`)를 임시 저장합니다.
 3. **Validation**: "저장 버튼을 눌러도 되는가?"(이미지 존재 여부 등)를 판단합니다.
 
 [설계 의도 및 이유 (Why)]
 1. 왜 별도로 분리했는가?
 - 작성 중인 데이터는 '저장'을 누르기 전까지는 확정된 데이터가 아닙니다.
 - 메인 뷰모델(`JourneyViewModel`)과 섞이지 않도록 독립적인 '임시 저장소'를 만들었습니다.
 
 2. 이미지/GPS 처리는?
 - 복잡한 로직은 나중에 붙입니다. 우선은 "이미지가 선택되었다", "좌표가 있다"는 상태값만 관리하도록 설계합니다.
 */


// MARK: - 1. State (입력 폼 상태)

/// 선택된 이미지 (화면 표시용)

/// 갤러리에서 선택된 아이템 (PhotosPicker 바인딩용)

/// 여정 날짜 (기본값: 오늘)

/// 메모 내용

/// 추출된 위치 정보 (없으면 nil)


// MARK: - 2. Validation (유효성 검사)

/// 저장 가능 여부 (이미지가 선택되어야 저장 가능)


// MARK: - 3. Actions (기능)

/*
 [handleImageSelection]
 - `imageSelection`이 변경되면 호출됩니다.
 - 여기서 `ImageEXIFHelper`를 사용하여 위치를 추출하고 `extractedLocation`에 저장하는 로직이 들어갈 자리입니다.
 - (지금은 상세 구현 생략, 역할만 정의)
 */
}
